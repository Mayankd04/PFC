#include <stdio.h>

int main() {
    int L;
    scanf("%d", &L);
    
    if (L >= 1 && L <= 5)
        printf("%d", L * 10);
    else
        printf("Invalid Level! 🚫", "\n");
        printf("Mayank dewangan");
    
    return 0;
}